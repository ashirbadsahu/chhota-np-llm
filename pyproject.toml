[project]
name = "chhota-np-llm"
version = "0.1.0"
description = "LLM from scratch using numpy and cupy"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cupy-cuda12x>=12.0.0",
    "ipykernel>=7.2.0",
    "numpy>=2.4.5",
]
